import { FADE_ENUM } from "./fade-enum";
import { ZOOM_ENUM } from "./zoom-enum";
import { ROTATE_ENUM } from "./rotate-enum";
import { MOVE_ENUM } from "./move-enum";
import { POSITION_ENUM } from "./position-enum";
import { FORCE_ORIGINAL_ASPECT_RATIO } from "./aspest-ratio-enum";


export const ENUM = {
  FADE_ENUM,
  ZOOM_ENUM,
  ROTATE_ENUM,
  MOVE_ENUM,
  POSITION_ENUM,
  FORCE_ORIGINAL_ASPECT_RATIO
};
